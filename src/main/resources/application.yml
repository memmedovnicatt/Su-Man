server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://db:5432/SuManDB

  jpa:
    hibernate:
      ddl-auto: update